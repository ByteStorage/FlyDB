package model

//Table Management: Store metadata about each table (such as column name, data type, index information, etc.)
//Record Storage: Responsible for storing records as key-value pairs. Each table has a unique key that maps to all records in the table.
//Indexing: Create and manage indexes for each indexed column. Each index has a key that maps to a list of records for each unique value in the indexed column.
